import React from "react";
import { Grid, Text, Input, Button } from "../elements/Index";

const Main = (props) => {
    return(
        <React.Fragment>
            <Grid padding="16px 8px">

            </Grid>
        </React.Fragment>
    );
}

export default Main;